#  搭建个人视频站

## [LibreSpark/LibreTv](https://github.com/LibreSpark/LibreTv)

**docker-compose.yml**

```dockerfile
services:
  libretv:
    image: bestzwei/libretv:latest
    container_name: libretv
    ports:
      - "8899:8080" # 将内部 8080 端口映射到主机的 8899 端口
    environment:
      - PASSWORD=${PASSWORD:-111111} # 可将 111111 修改为你想要的密码，默认为 your_password(只要将-号后面的数字改为你需要的密码即可)
    restart: unless-stopped
```

##  emby

https://hub.docker.com/r/emby/embyserver

https://emby.media/download.html

# 异地情侣一起看电影

## [synctv](https://github.com/synctv-org/synctv)

**docker-compose搭建**

```php
version: '3.3'
services:
  synctv:
    image: 'synctvorg/synctv:latest'
    container_name: synctv
    restart: unless-stopped
    ports:
      - '8081:8080/tcp'
      - '8081:8080/udp'
    volumes:
      - /opt/synctv:/root/.synctv
    environment:
      - PUID=0
      - PGID=0
      - UMASK=022
      - TZ=Asia/Shanghai
```

